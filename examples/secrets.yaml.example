# Copy this file to secrets.yaml and fill in your own values.
# secrets.yaml is in .gitignore so your credentials stay local.

upstream_ssid: "YourUpstreamSSID"
upstream_pass: "YourUpstreamPassword"
ap_pass: "YourRepeaterAPPassword"   # 8+ chars for WPA2; empty string for open AP
